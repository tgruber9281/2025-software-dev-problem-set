class Solution {
    public boolean containsDuplicate(int[] nums) {
        for (int i = 0; i < nums.length; i++) {
                    int intcheck = nums[i];
                    for (int h = i+1; h < nums.length; h++) {
                        if (intcheck == nums[h]) {
                            return true;
                        }
                    }
                }
                return false;
    }
}
